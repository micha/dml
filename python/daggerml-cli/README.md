# daggerml-cli

`daggerml-cli` provides the Python command-line application and installs the
`dml` entry point.

## Local build

From this directory:

```bash
python -m build
```

This produces a pure Python wheel and an sdist in `dist/`.
