# daggerml-adapter-example

`daggerml-adapter-example` is a scaffold adapter package that demonstrates
adapter entry point wiring.

## Local build

From this directory:

```bash
python -m build
```

This produces a pure Python wheel and an sdist in `dist/`.
